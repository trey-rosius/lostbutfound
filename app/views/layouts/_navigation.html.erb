  <nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Retrieve Lost Items", root_path,class: "navbar-brand",id:"logo"%>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    
      <ul class="nav navbar-nav">
        <li> <%= link_to "About",about_path%></li>
        <% if user_signed_in? %>
         
         <% else %>
           <li><%= link_to "Agents Location", locations_path%></li>  
        <% end %>
      
         <% if user_signed_in? and !current_user.active%>
          <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Manage Items<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Add New Item",new_product_path%></li>
            <li><%= link_to "View Lost Items",products_path%></li>
             
            
            <% end %>
          
          </ul>
        </li>
       
      </ul>
     
      <ul class="nav navbar-nav navbar-right">
      
        <% if user_signed_in? %>
        <li><%= link_to 'Search',root_path%></li>
         <li><%= link_to 'Log Out',destroy_user_session_path,method: :delete%></li>
            <% if user_signed_in? and current_user.admin %>
         <ul class="nav navbar-nav">
     
          <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Manage Agents <span class="caret"></span></a>
          <ul class="dropdown-menu">
             <li><%= link_to "Create New Agent", signup_path%></li>
            <li><%= link_to "View All Agents", users_path%></li>
            <li><%= link_to "Create Agent Location",new_location_path%></li>
            <li><%= link_to "View Agent Location",locations_path%></li>
          </ul>

        </li>
      </ul>
      <% end %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
         
          <i class="fa fa-user"> </i><%= "(Admin)" if current_user.admin%><%=current_user.full_name%>'s profile <span class="caret"></span></a>
           <% if !current_user.active %>
             <ul class="dropdown-menu">
             <li><%= link_to "Edit Your Profile",edit_user_registration_path(current_user)%></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to "View your profile", user_path(current_user)%></li>
            
          </ul>
           <% end %>
          
        </li>
        <% else %>
         <li><%= link_to 'Log in',  user_session_path%></li>
          
      <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
